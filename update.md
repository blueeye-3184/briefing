# Update Log (Release Notes)

이 파일은 프로젝트의 주요 업데이트 및 수정 사항을 기록하는 릴리즈 노트입니다.

## [2026-07-02] - 인용 정합성 강화 및 참고문헌 포맷 무결성 개량 (v7.1)
- **프롬프트 고도화 및 인용 규칙 강제**:
    - 2단계 생성 시 모델이 1단계에서 수집된 실제 논문 목록을 무시하고 자체 메모리에서 임의의 자료를 환각(Hallucination) 인용하던 문제를 차단했습니다. "1단계 수집 목록 100% 인용" 지침을 강력히 주입했습니다.
    - 참고문헌 양식 내 구글 스칼라, DBpia, RISS 3대 포털 하이퍼링크가 변수 플레이스홀더 그대로 누락되는 문제를 해결하기 위해 구체적인 템플릿 치환 원칙을 적용했습니다.
    - 1단계 스니펫의 핵심 데이터를 받아 적는 `[검색 결과 실측 증거 (Grounding Evidence)]` 표출 무결성을 완성했습니다.
    - 저널의 권/호/페이지 기재 배제 규칙을 재확인하여 교차 결합 환각을 영구 퇴치했습니다.
- **거버넌스 승인 및 검증**:
    - 본 변경 사항은 Tier 1 결정 사항(`Decision ID: T1-20260702-01`)으로 13인 위원회의 최종 비준을 획득했습니다.
    - `2026-07-02` 조경 디자인 주제의 실시간 실행 및 노션 발행 검증을 완료했으며, 5개 실측 수집 논문의 정합 인용과 출처/실측증거 표출 무결성을 확인했습니다.

## [2026-06-30] - 신뢰성 보장을 위한 2단계 RAG(검색 후 작성) 격리 아키텍처 도입 (v7.0)
- **2단계 RAG(Retrieval-then-Generation) 아키텍처 전환**:
    - 모델의 생성과 검색 단계가 동시 수행되면서 발생하던 가상 시뮬레이션(simulated) 텍스트 유출 및 인용 매칭 오류를 근본적으로 극복하기 위해 파이프라인을 2단계로 완전 격리했습니다.
    - **1단계(검색 수집)**: 구글 검색 도구를 활성화하여 실제 학술 DB의 실존 논문 정보 및 실측 스니펫(Grounding Evidence)만 수집하여 컨텍스트화합니다.
    - **2단계(보고서 작성)**: 1단계의 실제 정보만을 프롬프트로 제공하고, **검색 도구를 완전히 비활성화(use_tools=False)**하여 작성 단계에서의 새로운 허구 데이터의 개입이나 환각 현상을 100% 방지했습니다.
    - 시스템 내부적인 검색 흔적('simulated' 등)이 노션 본문에 유출되지 않도록 철저히 정제 규칙을 적용했습니다.
- **거버넌스 승인 및 검증**:
    - 본 변경 사항은 Tier 1 결정 사항(`Decision ID: T1-20260630-05`)으로 13인 위원회의 최종 비준을 획득했습니다.
    - 단위 테스트(9개) 및 실시간 노션 자동 저장 실측을 완료했습니다.

## [2026-06-30] - 실측 증거(Grounding Evidence) 명시 및 3대 학술 포털 통합 연동 (v6.9)
- **프롬프트 고도화**:
    - 리포트 인용 정보의 근본적 신뢰성 향상을 위해 모든 참고문헌 항목에 반드시 실제 검색 결과 스니펫에서 추출한 수치/요약문인 `[검색 결과 실측 증거 (Grounding Evidence)]`를 추가하도록 강제했습니다.
    - 참고문헌마다 구글 스칼라, DBpia, RISS의 3대 학술 포털 개별 검색 쿼리 하이퍼링크를 동시 제공(평문 한글 쿼리 방식)하여 원스톱 팩트체크 및 원문 확인이 가능하도록 설계했습니다.
- **거버넌스 승인 및 검증**:
    - 본 변경 사항은 Tier 1 결정 사항(`Decision ID: T1-20260630-04`)으로 13인 위원회의 최종 비준을 획득했습니다.
    - 단위 테스트(9개) 및 실시간 노션 자동 저장 실측을 완료했습니다.

## [2026-06-30] - URL-Encoding 오류 방지 및 한글 쿼리 하이퍼링크 표준화 (v6.8)
- **프롬프트 고도화**:
    - Gemini의 수작업 % 인코딩(%EA%B1...) 과정에서 발생하는 오타 및 하이퍼링크 주소 미스매치 장애를 방지하기 위해 % 인코딩 수행을 금지했습니다.
    - 브라우저 자동 인코딩을 신뢰하고 `q=논문제목+저자명` 평문 조합으로 링크를 생성하도록 표준화했습니다.
    - 권/호 및 페이지 범위(예: 34(1), 3-10)의 기재를 원천 금지하여 엉뚱한 타 논문의 메타데이터가 임의 결합되는 교차 결합 환각(Patchwork Hallucination) 현상을 차단했습니다.
- **거버넌스 승인 및 검증**:
    - 본 변경 사항은 Tier 1 결정 사항(`Decision ID: T1-20260630-03`)으로 13인 위원회의 최종 비준을 획득했습니다.
    - 단위 테스트(9개) 및 실시간 노션 자동 저장 실측을 완료했습니다.

## [2026-06-30] - 학술 출처 404 오류 방지를 위한 구글 스칼라 검색 링크 도입 (v6.7)
- **프롬프트 고도화 (방안 2 적용)**:
    - `briefing_auto.py` 내의 Gemini 생성 프롬프트(요구사항 4번 및 5번)를 수정하여, 인용 자료 표기 시 `[논문명 / 저자 / 수록학회 또는 출처 / 발행연도 / 구글 스칼라 검색 링크]` 형식을 명시했습니다.
    - 실제 상세 논문 URL이나 DOI를 정확히 모를 경우 발생할 수 있는 가짜 주소 생성(Hallucination) 및 404 에러를 방지하기 위해, `https://scholar.google.com/scholar?q=논문제목` 형식의 구글 스칼라 검색 쿼리 링크를 생성하고 띄어쓰기를 + 기호로 처리하도록 강제했습니다.
- **거버넌스 승인 및 검증**:
    - 본 변경 사항은 Tier 1 결정 사항(`Decision ID: T1-20260630-01`)으로 13인 위원회의 최종 비준을 획득했습니다.
    - 변경 완료 후 전체 단위 테스트(9개)가 정상적으로 성공(Passed)함을 확인했으며, 실제 노션 자동 저장 테스트를 통해 404 에러가 발생하지 않는 원클릭 학술 검색 링크 활성화를 검증 완료했습니다.

## [2026-06-22] - Slack 환경 변수 주입 및 예외 처리 시스템 강화 (v6.6)
- **Actions 파이프라인 무결성 강화**:
    - `briefing_auto.py`의 `run_daily_briefing` 메소드 예외 처리에서 `raise e`를 적용하여 Silent Success(오류 발생 시에도 Actions가 성공으로 뜨는 현상) 문제를 완전 격퇴.
    - GitHub Actions 워크플로우 `.github/workflows/daily_briefing.yml`에 `SLACK_WEBHOOK_URL` 환경 변수를 정식 주입하여 자동 실행 시 Slack 실시간 보고 가동.
- **모델 및 테스트 호환성 정비**:
    - 2026년 6월 기준 변경된 API 모델 리스트(`models/gemini-pro-latest` 추가 및 `models/gemini-2.5-flash-lite` 제거) 반영.
    - Notion의 `blocks/children` 조회 방식으로 바뀐 폴더 체크 알고리즘에 맞추어 `tests/test_gemini_provider.py` 및 `tests/test_notion_infrastructure.py` 모의 객체(Mock) 로직 완전 개편 (9개 단위 테스트 100% 통과 확보).
- **거버넌스 점검 및 중복 식별**:
    - 노션 API 트리 조회를 통해 기존 글로벌 검색 버그로 인해 `2026년 04월` 밑에 잘못 안착된 중복 `2026년 06월` 폴더와 Root 하위의 정상 `2026년 06월` 폴더 간의 간섭 현상 규명. (6월 11일 ~ 6월 21일 이력은 정상 위치에 누락 없이 누적 기록되었음을 파악)

## [2026-06-10] - 거버넌스 완결 및 공공 배포 버전 (Public Release v6.5)
- **13인 적대적 검증 위원회(Adversarial Reviewers) 통합**:
    - **거버넌스 최신화**: `01_Standard_Procedures` 규준에 따라 기존 7인 위원회를 13인 체제로 대폭 확장.
    - **적대적 업무 명문화**: 13인의 페르소나별 핵심 직무 및 적대적 공격 임무를 README.md에 전수 반영.
- **실시간 무결성 검증 시스템 구축**:
    - **Governance Verification Matrix**: 모든 노션 리포트 하단에 프로토콜 준수 여부 및 사용 모델 정보 배지 자동 부착.
    - **공개 감사 로그**: `03.Committee_Opinions.md`(전략 비준) 및 `04.Data_Collection_Log.md`(실행 로그) 신설로 투명성 확보.
- **노션 연동 무결성 보정 (Regression Spec)**:
    - **계층 구조 정밀화**: 부모 ID 대조 방식을 통해 연도/월/주차 폴더 간의 논리적 고립 및 정확한 안착 보장.
    - **UI 일관성 복구**: 누락되었던 페이지 제목의 날짜/요일 접두사(`[YYYY-MM-DD(요일)]`) 포맷 완전 복구.
- **시스템 복구 및 최신화**:
    - VS Code 업데이트로 인한 유실 파일(테스트 수트, 로그 등) 100% 복구 및 최신화.
    - Gemini API `v1beta` 엔드포인트 최적화 및 2026년형 주력 모델(`2.5-flash` 등) 리스트 갱신.
- **검증 완료**: 9개 단위 테스트 통과 및 실시간 노션 자동 기록 성공 확인.


## [2026-04-22] - 시스템 안정화 및 노션 구조 동기화 (Hotfix & Sync)
- **Gemini API 400 INVALID_ARGUMENT 해결**:
    - **원인 분석**: `v1` API에서 `tools` 필드 인식 오류 및 Search Grounding 호환성 문제 확인.
    - **해결 조치**: 
        - API 버전을 **`v1beta`**로 전환하여 최신 도구 스택 확보.
        - 도구 호출 실패 시 자동으로 **도구 제외 재시도(Fallback)** 로직을 추가하여 생성 무결성 보장.
- **노션 트리 구조 실사 및 동기화**:
    - **실사 진행**: `inspect_notion.py`를 통한 실제 노션 계층 구조 전수 조사 완료.
    - **명칭 최적화**: 사용자 실사 결과에 맞춰 폴더 규칙(`X주차 분석`) 및 페이지 제목(`요일 주간 보고: 주제`) 동기화.
    - **중복 방지 강화**: 기존 폴더 재사용 로직을 실무 데이터에 맞게 보정하여 구조적 상이점 해결.
- **인프라 안정화**:
    - `requirements.txt` 생성: 로컬과 GitHub Actions 환경의 라이브러리 버전 완전 일치화.
    - 깃허브 전용 재시도 전략 상향: 429 에러 대응을 위해 재시도 횟수 및 대기 시간 확장.

## [2026-04-21] - 리포트 고도화 (5,000자 확대 및 학술 DB 검색 도입)
- **리포트 분량 및 품질 대폭 강화**:
    - **분량 확대**: 기존 1,200자 내외에서 **5,000자 내외의 심층 대하 리포트**로 사양 상향 조정.
    - **전문성 강화**: 단순 요약을 넘어 개요, 기술 배경, 사례 연구, 향후 10년 전망 등 논문 수준의 목차 구성 강제.
- **실시간 학술 검색(Google Search Grounding) 도입**:
    - **신뢰도 확보**: Gemini의 실시간 검색 기능을 활성화하여 허위 링크(Hallucination) 방지 및 최신 팩트 반영.
    - **학술 DB 타겟팅**: 대한건축학회(AIK), DBpia, RISS 등 국내 핵심 학술 데이터베이스를 우선적으로 참조하도록 프롬프트 고도화.
- **Notion 업로드 및 인프라 최적화**:
    - **TypeError 해결**: `publish_report` 호출부의 인자 구조 오류 수정으로 게시 안정성 확보.
    - **보안 및 환경 정리**: `.env` 보안 처리, `python-dotenv` 도입 및 GitHub Actions 워크플로우 의존성 업데이트 완료.


## [2026-04-17] - 404 NOT_FOUND 근본적 해결 및 2026년형 모델 세대 교체
- **API 버전 및 모델 전략 전면 개편**:
    - **원인 분석**: 구글의 Gemini 1.5 시리즈 단종(Shutdown) 및 `v1beta` 엔드포인트의 불안정성 확인.
    - **해결 조치**: 
        - `http_options`를 통해 **`v1` (Stable) API 버전** 사용 명시.
        - **Gemini 3.1 및 2.5 시리즈**를 주력 모델로 도입 (2026년 표준 대응).
- **4단계 폴백(Fallback) 방어선 구축**:
    - `gemini-3.1-flash` → `gemini-3.1-flash-lite` → `gemini-2.5-flash` → `gemini-1.5-flash-latest` 순으로 시도하여 실행 무결성 확보.
- **개발 환경 최적화**:
    - `pyrightconfig.json` 수정: 불필요한 분석 폴더(`node_modules`, 숨김 폴더)를 제외하여 Pylance 성능 향상 및 경고 해결.
- **검증 완료**: 2026년 모델 대응 단위 테스트 100% 통과 확인.

## [2026-04-16] - 404 오류 해결 및 테스트 자동화 강화 (Hotfix)
- **모델 명칭 긴급 수정**: 
    - **원인 분석**: 존재하지 않는 모델명(`gemini-1.5-flash-lite`) 사용으로 인한 **404 NOT_FOUND** 오류 해결.
    - **해결 조치**: SDK 공식 지원 모델인 **`gemini-1.5-flash`** 및 **`gemini-1.5-flash-8b`**로 교체 완료.
- **테스트 주도 안정성 확보 (TDD)**:
    - `tests/test_gemini_provider.py` 추가: 모델명 유효성 및 폴백(Fallback) 로직에 대한 단위 테스트 구현.
    - 향후 모델 변경 시 발생할 수 있는 런타임 오류를 사전에 차단하는 회귀 방지 체계 구축.
- **7인 위원회 승인**: 404 핫픽스 및 자동화된 검증 로직 도입에 대한 최종 배포 승인 획득.

## [2026-04-15] - 429 오류 해결을 위한 모델 최적화 및 안정화
- **모델 전략 전면 수정**: 
    - **원인 분석**: 2.0 시리즈(`gemini-2.0-flash`)의 무료 티어 할당량 부족(Quota 0)으로 인한 2일 연속 브리핑 실패 확인.
    - **해결 조치**: PDF 분석 및 7인 위원회 승인을 통해 할당량이 넉넉한(일 1,500회) **`gemini-1.5-flash-lite`**를 기본 모델로, **`gemini-1.5-flash`**를 폴백 모델로 설정.
- **브리핑 복구 완료**:
    - 주제: "설계 자동화 기술 및 BIM 최신 동향"에 대한 브리핑 생성 환경 정상화.
    - `briefing_auto.py` 내 모델 리스트 업데이트 및 독스트링 수정 완료.
- **시스템 안정성 확보**: 
    - 넉넉한 Quota를 기반으로 지수 백오프(Exponential Backoff) 로직이 결합되어 사실상 429 오류로부터 자유로운 환경 구축.

## [2026-04-14] - GitHub 동기화 및 404/429 대응 안정화
- **상태 동기화**: `origin/main`의 최신 릴리즈 버전을 로컬 저장소에 완벽하게 반영 완료.
- **주요 수정 사항**:
    - **404 Error Fix**: Gemini API 호출 시 `2.0-flash` 모델 버전 변경으로 인한 404 오류 해결.
    - **429 Rate Limit Fix**: API 호출 빈도 초과 시 자동으로 재시도(Retry)를 수행하여 대량 작업 안정성 확보.
- **환경 관리**:
    - 불필요한 캐시 및 프로필 데이터(`gemini_web_profile/`, `keep_profile/` 등)를 Git 추적에서 제외하여 저장소 최적화.
    - `pytest`를 통한 Gemini 로직 검증 완료 (9개 테스트 항목 100% 통과).
- **릴리즈 노트 도입**: `briefing/update.md`를 통해 변경 이력을 관리하기 시작함.

## [2026-04-14] - GitHub Actions 실행 이슈 보고
- **이슈 발견**: GitHub Actions 자동 실행 중 **429 (RESOURCE_EXHAUSTED)** 오류 발생 확인.
- **분석 결과**: 
    - 주제: "구미/김천 지역 부동산 정책 및 시장 흐름 분석"
    - 원인: Gemini API 무료 티어(Free Tier)의 일일 또는 분당 할당량(Quota) 초과.
    - 조치: 총 4회의 자동 재시도 로직이 정상 작동했으나, 할당량 자체가 0이 된 상태여서 최종 실패함.
- **대응 권장**: API 키의 할당량 상태 확인 및 실행 시간대 조정 필요.

## [2026-04-14] - 시스템 구조 고도화 (DDD/TDD 적용)
- **KST(한국 표준시) 도입**: 
    - 서버 위치와 관계없이 항상 한국 시간을 기준으로 요일을 판별하도록 수정.
    - `datetime.now(KST)`를 적용하여 18번 줄의 요일별 주제 불일치 문제 해결.
- **Notion 중복 생성 방지**:
    - `find_page_by_title` (Search API) 기능을 추가하여 기존에 생성된 월/주차 페이지가 있을 경우 이를 재사용하도록 로직 개선.
    - 매일 실행 시 노션 워크스페이스가 지저분해지는 문제 해결.
- **마크다운 변환 로직 개선**:
    - Gemini가 생성한 리포트의 불렛 리스트(`-`), 번호 리스트(`1.`)를 노션의 전용 블록 타입으로 변환하여 가독성 향상.
- **테스트 주도 개발(TDD) 환경 구축**:
    - `pytest`를 활용한 3개 영역(도메인, 인프라, 변환 로직)에 대한 단위 테스트 6개 추가 및 통과 확인.
    - 향후 유지보수를 위한 견고한 코드 베이스 확보.

## [2026-04-14] - 타입 무결성 확보 및 Pylance Strict Mode 최적화 (최종 완성)
- **정적 타입 체크 0개 달성**: 
    - VS Code Pylance의 `strict` 모드에서 보고된 106개의 타입 문제를 모두 해결.
    - `isinstance` 기반 타입 가드와 명시적 `cast`를 통해 런타임 안정성 극대화.
- **Pylance 개발 환경 최적화**:
    - `pyrightconfig.json` 설정을 통해 외부 라이브러리(genai, tenacity)의 타입 부재 문제를 논리적으로 우회.
    - 로컬 변수 캡처(Local Variable Capture) 기법을 사용하여 `Optional` 객체의 속성 접근 에러 근본적 차단.
- **DDD/TDD 아키텍처 완성**:
    - 도메인(비즈니스 규칙), 인프라(외부 연동), 애플리케이션(조율) 레이어를 명확히 분리.
    - 7인 위원회의 엄격한 논리 검증을 통과한 배포 승인 버전 확정.
- **3중 에러 방어 체계 구축**:
    - **모델 폴백(Fallback)**: `gemini-2.0-flash` 실패 시 `gemini-2.0-flash-lite`로 즉시 전환하여 실행 연속성 확보.
    - **랜덤 지터(Random Jitter)**: 실행 시작 시 0~300초 무작위 대기를 적용하여 전 세계 트래픽 피크 타임 회피.
    - **지수 백오프(Exponential Backoff)**: 재시도 간격을 최대 10분까지 늘려 API 할당량 회복 시간 확보.
- **리포트 품질 고도화**:
    - **🔗 자료 출처 포함**: 분석의 근거가 되는 뉴스 및 공식 문서 웹 링크(2~3개)를 자동으로 첨부하도록 프롬프트 개선.
    - **노션 블록 분할**: 블록당 2,000자 제한 문제를 해결하기 위해 긴 텍스트를 자동으로 분할 생성하는 로직 적용.
- **워크플로우 최적화**:
    - 기존 요일별 파일을 `daily_briefing.yml`로 통합하고, 실행 시간을 **KST 오전 08:17**로 조정하여 안정성 극대화.

## [2026-04-14] - 프로젝트 완전 모듈화 및 최종 승인 (Grand Final)
- **독립적 프로젝트 구조 완성**: 
    - `pyrightconfig.json`, `.env`, `tests/` 폴더를 `briefing/` 내부로 통합하여 폴더 하나로 작동하는 완전 모듈화 달성.
    - 프로젝트 루트(`.`) 환경 정리를 통한 Git 클린 상태 확보.
- **7인 위원회 최종 검증 통과**:
    - CEO, CTO를 포함한 7인의 이해관계자로부터 DDD/TDD 및 타입 무결성에 대한 '최종 배포 승인' 획득.
    - 논리적 설계 무결성 및 실무 활용성 최종 입증.
- **보안 및 자원 최적화**:
    - `.gitignore` 고도화를 통해 대용량 프로필 및 보안 민감 파일(.env)의 외부 유출 원천 차단.

## [2026-04-14] - 저장소 구조 평탄화 및 최종 배포 (Final Release)
- **중첩 구조 제거**: 
    - `briefing/` 폴더 내의 모든 자산을 프로젝트 루트(`.`)로 이동시켜 깃허브 저장소 가독성 극대화.
    - 불필요하게 중복되었던 하위 폴더 및 캐시 파일 완전 정리.
- **워크플로우 경로 동기화**:
    - 파일 이동에 맞춰 `daily_briefing.yml`의 실행 경로를 루트 기준으로 재설정하여 자동화 무결성 확보.
- **설정 파일 최적화**:
    - `pyrightconfig.json`을 루트 기준으로 재설정하여 전체 프로젝트에 대한 Strict Type Check 유지.
- **Grand Final Status**: 7인 위원회의 승인을 받은 최종 아키텍처가 깃허브 메인 저장소에 완벽히 정착됨.


